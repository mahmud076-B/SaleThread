import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["duh-sermon-frisbee.ngrok-free.dev", "*.ngrok-free.dev"],
};

export default nextConfig;
